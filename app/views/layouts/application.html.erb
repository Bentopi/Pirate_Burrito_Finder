<!DOCTYPE html>
<html>
<head>
  <title>Burrito Plunderer</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <%= link_to root_path do %>
        <%= image_tag("burrito_logo", class: "navbar-brand") %>
      <% end %>
    </div>
    <div class="navbar-text">
      <span class="burrito-find" >Ye'd be a Bilge-Sucking Scallywag not to plunder a Burrito! <small class="quote"> ~Capt. Guacbeard</small></span>
    </div>
    <div class="navbar-right sign-in">
      <% if @current_user %>
      <div class="navbar-text">
        Signed in as <%= @current_user.email %>
        (<%= link_to "Sign Out", sign_in_path, method: :delete %>)
      </div>
      <% else %>
          <%= link_to "Sign In", sign_in_path, class: "btn btn-primary navbar-btn" %>
      <% end %>
    </div>
    <div class="navbar-text navbar-right reset-btn">
      <%= link_to "Reset Map", reset_map_path, class: "btn btn-danger navbar-btn" %>
    </div>
  </div>
</nav>
<!-- <div class="container notices">
  <% if notice %>
    <div class="alert alert-info" role="alert">
      <%= notice %>
    </div>
  <% end %>

  <% if alert %>
    <div class="alert alert-danger" role="alert">
      <%= alert %>
    </div>
  <% end %>
</div> -->
<%= yield %>

</body>
</html>
